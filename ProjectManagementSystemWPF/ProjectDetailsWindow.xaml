<Window x:Class="ProjectManagementSystem.ProjectDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Project Details" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Name" FontWeight="Bold" />
            <TextBlock Text="{Binding Name}" Margin="0,0,0,10" />

            <TextBlock Text="Description" FontWeight="Bold" />
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Delete" Width="75" Margin="0, 0, 10, 0" Click="Delete_Click" />
                <Button Content="Update" Width="75" Click="Update_Click" />
            </StackPanel>

            <TextBlock Text="Tasks" FontWeight="Bold" Margin="0, 10, 0, 5" />
            <ListBox Name="TasksListBox" Height="150" Margin="10"
                     SelectionChanged="TasksListBox_SelectionChanged"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                            <TextBlock Text="{Binding Description}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Content="Add" Width="75" Click="Add_Click" HorizontalAlignment="Right" />
        </StackPanel>
    </Grid>
</Window>
